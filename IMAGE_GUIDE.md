# 图片资源替换指南

## 📋 目录
- [图片文件说明](#图片文件说明)
- [如何替换图片](#如何替换图片)
- [图片规格要求](#图片规格要求)
- [当前使用的占位图](#当前使用的占位图)
- [图片引用位置](#图片引用位置)

---

## 图片文件说明

### assets/images/ 目录
```
assets/images/
├── logo.png              # 导航栏 Logo（已复制）
├── lab-logo.png          # 页面展示 Logo（已复制）
├── lab-icon.png          # 实验室图标（已复制）
├── member-avatar.png     # 成员头像占位图（已复制）
├── news-default.png      # 新闻默认图片（已复制）
├── blog-default.png      # 博客默认图片（已复制）
├── project-default.png   # 项目默认图片（已复制）
├── activity-default.png  # 活动默认图片（已复制）
└── 实验室图标.png       # 原始图标文件（保留）
```

### assets/backgrounds/ 目录
```
assets/backgrounds/
├── default-bg.jpg       # 默认背景图（已复制）
├── nature.jpg           # 自然风格背景（已复制）
└── tech.jpg            # 科技风格背景（已复制）
```

---

## 如何替换图片

### 方法一：直接替换（简单）

#### 1. 替换 Logo
```bash
# 将你的 Logo 文件重命名为 logo.png
cp /path/to/your-logo.png assets/images/logo.png

# 同时更新 lab-logo.png 和 lab-icon.png
cp /path/to/your-logo.png assets/images/lab-logo.png
cp /path/to/your-logo.png assets/images/lab-icon.png
```

#### 2. 替换背景图片
```bash
# 替换默认背景
cp /path/to/your-bg.jpg assets/backgrounds/default-bg.jpg

# 替换自然风格背景
cp /path/to/your-nature.jpg assets/backgrounds/nature.jpg

# 替换科技风格背景
cp /path/to/your-tech.jpg assets/backgrounds/tech.jpg
```

### 方法二：使用文件管理器

1. 找到项目的 `assets/images/` 文件夹
2. 直接替换对应的图片文件
3. 保持文件名不变
4. 刷新浏览器查看效果

### 方法三：在数据文件中引用

如果需要为特定内容添加图片，在对应的 JSON 文件中引用：

#### 新闻图片
```json
{
  "id": 1,
  "title": "新闻标题",
  "image": "assets/images/news-1.png",  // 添加图片字段
  "content": "内容..."
}
```

#### 博客图片
```json
{
  "id": 1,
  "title": "博客标题",
  "systemImage": "assets/images/blog-1.png",  // 添加图片字段
  "content": "内容..."
}
```

#### 刊物封面
```json
{
  "id": 1,
  "title": "书名",
  "coverImage": "assets/images/publication-1.png",  // 添加封面图片
  "description": "描述..."
}
```

---

## 图片规格要求

### Logo 图片
- **格式**: PNG（推荐）或 SVG
- **尺寸**: 建议 200x200px 或 256x256px
- **背景**: 透明背景（PNG）
- **文件大小**: 小于 500KB
- **用途**: 导航栏、页脚

### 实验室 Logo
- **格式**: PNG 或 JPG
- **尺寸**: 建议 400x400px 或更大
- **背景**: 可选透明或白色背景
- **文件大小**: 小于 1MB
- **用途**: Hero 区域、页面标题

### 背景图片
- **格式**: JPG 或 PNG
- **尺寸**: 建议 1920x1080px 或更大
- **文件大小**: 小于 2MB
- **用途**: Hero 区域背景、自定义背景

### 成员头像
- **格式**: PNG（推荐）
- **尺寸**: 建议 200x200px 或 256x256px
- **背景**: 透明背景或圆形裁切
- **文件大小**: 小于 500KB
- **用途**: 团队成员展示

### 内容图片
- **格式**: JPG 或 PNG
- **尺寸**: 建议 800x600px 或 1200x800px
- **文件大小**: 小于 2MB
- **用途**: 新闻、博客、项目展示

---

## 当前使用的占位图

### 已复制的占位图
所有以下位置都已使用 `实验室图标.png` 作为占位图：

1. **assets/images/logo.png** - 导航栏 Logo
2. **assets/images/lab-logo.png** - 页面 Logo
3. **assets/images/lab-icon.png** - 实验室图标
4. **assets/images/member-avatar.png** - 成员头像占位
5. **assets/images/news-default.png** - 新闻默认图
6. **assets/images/blog-default.png** - 博客默认图
7. **assets/images/project-default.png** - 项目默认图
8. **assets/images/activity-default.png** - 活动默认图
9. **assets/backgrounds/default-bg.jpg** - 默认背景
10. **assets/backgrounds/nature.jpg** - 自然背景
11. **assets/backgrounds/tech.jpg** - 科技背景

### 替换建议
- **优先替换**: logo.png, lab-logo.png（最重要）
- **其次替换**: 背景图片（影响视觉体验）
- **可选替换**: 其他占位图（不影响功能）

---

## 图片引用位置

### 1. HTML 中引用
```html
<!-- 导航栏 Logo -->
<img src="assets/images/logo.png" alt="Logo" class="brand-logo">

<!-- 成员头像（动态生成） -->
<div class="placeholder-avatar">张</div>
```

### 2. CSS 中引用
```css
/* Hero 区域背景 */
.hero {
    background-image: url('assets/backgrounds/tech.jpg');
    background-size: cover;
}
```

### 3. JavaScript 中引用
```javascript
// 动态加载图片
const imageUrl = 'assets/images/news-' + newsId + '.png';
```

### 4. JSON 数据中引用
```json
{
  "image": "assets/images/news-default.png"
}
```

---

## 图片分类存储建议

### 推荐的文件夹结构
```
assets/
├── images/                    # 所有图片
│   ├── logos/                # Logo 相关
│   │   ├── main-logo.png      # 主 Logo
│   │   └── favicon.ico       # 网站图标
│   ├── avatars/              # 头像图片
│   │   ├── members/          # 成员头像
│   │   │   ├── 1.png
│   │   │   ├── 2.png
│   │   │   └── ...
│   │   └── placeholder.png   # 占位头像
│   ├── news/                # 新闻图片
│   │   ├── 1.png
│   │   ├── 2.png
│   │   └── ...
│   ├── blogs/               # 博客图片
│   │   └── ...
│   ├── projects/            # 项目图片
│   │   └── ...
│   └── activities/          # 活动图片
│       └── ...
└── backgrounds/            # 背景图片
    ├── default/
    │   └── default.jpg
    ├── nature/
    │   └── nature.jpg
    └── tech/
        └── tech.jpg
```

### 当前简化结构
```
assets/
├── images/               # 所有图片混放（当前）
│   ├── logo.png
│   ├── lab-logo.png
│   ├── member-avatar.png
│   └── ...
└── backgrounds/          # 背景图片单独存放
    ├── default-bg.jpg
    ├── nature.jpg
    └── tech.jpg
```

---

## 常见问题

### Q1: 替换图片后不显示？
**A**:
1. 检查文件名是否完全一致（区分大小写）
2. 检查图片格式是否支持
3. 强制刷新浏览器（Ctrl+F5）
4. 清除浏览器缓存

### Q2: 图片显示模糊？
**A**:
1. 使用高分辨率图片
2. 确保 CSS 中设置了正确的尺寸
3. 使用 SVG 格式获得最佳质量

### Q3: 背景图片不合适？
**A**:
1. 使用大尺寸图片（至少1920x1080px）
2. 确保图片质量良好
3. 调整 `background-size: cover` 属性

### Q4: 如何为每个新闻添加不同图片？
**A**:
1. 在 `assets/images/` 中创建对应编号的图片
2. 在 `data/news/news.json` 中添加 `image` 字段
3. 修改 `script.js` 中的渲染逻辑

### Q5: 成员如何上传自己的头像？
**A**:
当前版本使用文字头像（姓氏）。如需图片头像：
1. 在 `assets/images/` 中创建 `member-{id}.png`
2. 修改 `script.js` 中的头像渲染逻辑
3. 使用 `<img>` 标签替代文字

---

## 下一步

1. **立即替换** Logo 和背景图片
2. **测试显示** 确保所有图片正确加载
3. **优化图片** 压缩图片以提高加载速度
4. **备份原图片** 保留原始占位图

---

**最后更新**: 2024-01-30
**版本**: v1.0.0
